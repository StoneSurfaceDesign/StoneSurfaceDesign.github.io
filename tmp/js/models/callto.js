// tmp/js/models/callto.js

var app = app || {};

// CallTo Model
// -----------
// Our basic **CallTo** model

app.CallTo = Backbone.Model.extend({

	// Default attributes
	defaults: {
		callToAct: 'Unknown'
		
	}

});